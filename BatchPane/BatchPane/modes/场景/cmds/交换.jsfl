${前期处理	var selectedItems=fl.getDocumentDOM().library.getSelectedItems();	switch(selectedItems.length){		case 0:			alert("请先选择要交换为的库元件");			return;		break;		case 1:			var swapItemName=selectedItems[0].name;		break;		default:			alert("请只选择一个要交换为的库元件");			return;		break;	}前期处理}	fl.getDocumentDOM().selectNone();	element.selected=true;	fl.getDocumentDOM().swapElement(swapItemName);